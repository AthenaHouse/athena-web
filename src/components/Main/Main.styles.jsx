import { styled } from "styled-components";

export const $Main = styled.main`
  max-width: 100%;
  display: flex;
  justify-content: center;
  margin: 0px auto;
  padding-bottom: 10px;
`;

export default $Main;